# This configuration is intended for development purpose, it's **your** responsibility to harden it for production
name: parteedashboard
services:
  mariadb:
    extends:
      file: ./mariadb.yml
      service: mariadb
  consul:
    extends:
      file: ./consul.yml
      service: consul
  consul-config-loader:
    extends:
      file: ./consul.yml
      service: consul-config-loader
  kafka:
    extends:
      file: ./kafka.yml
      service: kafka
